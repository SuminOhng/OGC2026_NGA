# Configuration

This folder is included in the submission archive. Keep configuration files
small, deterministic, and general across hidden instances.

Do not store experiment results, local paths, Gurobi license files, known
solutions, or training-instance caches here. The solver must remain valid when
the archive is evaluated on unseen problems.
